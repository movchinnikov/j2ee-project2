spring:
  application:
    name: pricing-service

  datasource:
    url: jdbc:postgresql://localhost:15432/pricing_db
    username: pricing_user
    password: pricing_secret_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: pricing

  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: pricing
    default-schema: pricing
    baseline-on-migrate: true
    validate-on-migrate: true
    repair-on-migrate: true

server:
  port: 18082

jwt:
  secret: "J2EE-IAC-Service-Super-Secret-Key-2024-Change-This-In-Production!!"

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, info

logging:
  level:
    com.project.pricing: DEBUG
